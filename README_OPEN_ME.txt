This version was patched to avoid the file:// module-loading issue in Chrome.

You can use it in either way:
1) Double-click index.html
2) Or run START_LOCAL_SERVER.bat and open http://localhost:8000
